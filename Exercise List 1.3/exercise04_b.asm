# As the the j considers the first 4 bits of the adress, and the PC is 0x0000 0000, the first hex digit is
# is 0, with a unique jump it can reach only spots that as well start with 0

# For the first value, it starts with 2, so the jump CAN'T jump with a unique jump with this case

# For the second value, it starts with 0, so here the jump CAN use a unique jump