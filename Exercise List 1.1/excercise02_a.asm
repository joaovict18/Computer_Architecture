# O programa exibe uma mensagem solicitando um número inteiro ao usuário, depois que o usuário digita
# é executado uma operação de shift à esquerda, movimentando 2 bits, isto é, multiplicando por 4 e ao
# fim, é exibido o resultado.